apiVersion: v1
kind: ServiceAccount
metadata:
  name: vllm
  namespace: vllm
  annotations:
    # IRSA annotation — replaced with your actual role ARN from Terraform output
    eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/mlops-eks-vllm-role"
  labels:
    app.kubernetes.io/name: vllm
    app.kubernetes.io/component: inference
